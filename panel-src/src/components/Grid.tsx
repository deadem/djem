import { Proxy, Http, State, Store } from '../store/Proxy';
import Mui from './Mui';

class Grid extends Proxy {
  render() {
    return (
      <div className='Grid'>

      </div>
    );
  }
}

export default Grid;
