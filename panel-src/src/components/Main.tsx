import Login from './Login';
import Toolbar from './Toolbar';

class Main extends React.Component {
  render() {
    return (
      <div>
        <Login />
        <Toolbar />
      </div>
    );
  }
}

export default Main;
