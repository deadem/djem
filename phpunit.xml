<phpunit>
  <filter>
        <whitelist>
            <directory suffix=".php">./app</directory>
            <exclude>
                <directory>./vendor</directory>
                <directory>./tests</directory>
            </exclude>
        </whitelist>
    </filter>
</phpunit>